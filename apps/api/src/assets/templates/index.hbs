<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{action}}</title>
    <style>
        body{
            display: grid;
            grid-auto-flow: row;
            min-height: 100vh;
            justify-content: center;
        }
        .body{
            background-color: white;
            box-shadow: 0px -1px 7px rgba(0, 0, 0, 0.25);
            display: grid;
            justify-content: center;
            text-align: center;
        }
        h1{
            color: blueviolet;
        }
        .link_button{
            box-shadow: 0px -1px 7px rgba(0, 0, 0, 0.25);
            width: 150px;
            height: 50px;
            border-radius: 15px;
            justify-self: center;
            text-align: center;
        }
        a{
            color: white;
        }
    </style>
</head>
<body >
    <img width="150" height="100" src={{logo}} alt="draivway.com"/>
    <div class="body">
        <h1>
            {{subject}}
        </h1>
        <p>{{message}}</p>
        <button class="link_button">
            <a href={{link}}>
                {{link_message}}
            </a>
        </button>
        <p>
            Thank you for {{action}} on Hope
        </p>
    </div>
</body>
</html>
