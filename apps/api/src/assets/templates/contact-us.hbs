<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{action}}</title>
    <style>
        body {
            display: grid;
            gap: 15px;
            grid-auto-flow: row;
            min-height: 100vh;
            justify-content: center;
        }

        .body {
            background-color: white;
            display: grid;
        }

        h1 {
            color: #009C7D;
        }

        .link_button {
            box-shadow: 0px -1px 7px rgba(0, 0, 0, 0.25);
            width: 150px;
            height: 50px;
            border-radius: 15px;
            justify-self: center;
            text-align: center;
        }

        a {
            color: white;
        }
    </style>
</head>

<body>
    <img width="150" height="100" src={{logo}} alt="draivway.com" />
    <h1>{{subject}}</h1>
    <div class="body">
        <p>{{message}}</p>
        <p>{{messageDisclaimer}}</p>
        <p>{{actionText}}<a href="mailto:{{email}}">{{email}}</a></p>
    </div>
    <div class="footer">
        <p class="footer">Hope Home est une solution de exposition des biens et proprietés immobilières.
            @Developeurs GLOM
        </p>
    </div>
</body>

</html>